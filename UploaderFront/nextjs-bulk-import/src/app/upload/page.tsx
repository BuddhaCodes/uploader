import BulkImportTerminal from '@/components/BulkImportTerminal';

export const metadata = {
  title: 'Terminal de carga — Bulk Import',
};

export default function UploadPage() {
  return <BulkImportTerminal />;
}
